import definitions from '../data/hiddenCostPatterns.json' with { type: 'json' };
const amountPattern=/(?:₹|INR|Rs\.?|USD|\$)\s*[\d,]+(?:\.\d{1,2})?|\b\d+(?:\.\d+)?%/gi;
export function detectHiddenCosts(clause){const normalized=clause.toLowerCase().replace(/[-_/]+/g,' ').replace(/\s+/g,' '),amounts=clause.match(amountPattern)||[],matches=[];for(const item of definitions){const term=item.keywords.find(k=>normalized.includes(k))||item.patterns.find(p=>new RegExp(p,'i').test(normalized));if(term)matches.push({cost_id:item.id,label:item.label,matched_term:term,amounts,warning:'This fee may not be legally required and may be exploitative. Verify its contractual basis, necessity, calculation, and applicable law before paying.',source_excerpt:clause.slice(0,240)})}return matches}
