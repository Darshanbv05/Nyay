import libraries from '../data/penaltyRulesets.json' with { type: 'json' };
const money=/(?:₹|INR|Rs\.?|USD|\$)\s*[\d,]+(?:\.\d{1,2})?(?:\s*(?:per day|per month|daily|monthly))?/gi,percent=/\b\d+(?:\.\d+)?%\s*(?:per annum|p\.a\.|per month|monthly|daily)?/gi;
export function trackPenalties(clause,type='rental'){const lower=clause.toLowerCase(),rule=(libraries[type]||[]).find(r=>r.keywords.some(k=>lower.includes(k))),amounts=[...(clause.match(money)||[]),...(clause.match(percent)||[])];if(!rule&&!/(penalt|forfeit|compensat|damages|late fee|service credit)/i.test(clause))return{penalty_items:[],compensation_items:[],penalty_fairness:null,penalty_rule_id:null};const side=rule?.liable_side||(/landlord|lender|employer|service provider/i.test(clause)?'recipient':'applicant'),item={trigger:rule?.trigger||'The stated obligation is not met',consequence:`${rule?.consequence||'Contractual consequence'} — ${amounts.length?amounts.join(', '):'No amount is expressly stated'}`,amounts,liable_side:side};return{penalty_items:side==='applicant'?[item]:[],compensation_items:side==='recipient'?[item]:[],penalty_fairness:rule?.fairness||'Check whether this consequence is clear, mutual, capped, and proportionate.',penalty_rule_id:rule?.id||'document_penalty',penalty_risk_level:rule?.risk_level||'medium'}}
