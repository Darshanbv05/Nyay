import libraries from '../data/obligationRulesets.json' with { type: 'json' };
export function detectDocumentType(text){const lower=text.toLowerCase(),best=Object.entries(libraries).map(([type,lib])=>({type,score:lib.detection_keywords.filter(k=>lower.includes(k)).length})).sort((a,b)=>b.score-a.score)[0];return best?.score?best.type:'general'}
export function mapObligations(clause,type){const generic={applicant_label:'Party A',recipient_label:'Party B',defaults:{applicant:['Perform the obligations assigned to Party A in the agreement'],recipient:['Perform the obligations assigned to Party B in the agreement']},patterns:[]},lib=libraries[type]||generic,lower=clause.toLowerCase(),p=lib.patterns.find(x=>x.keywords.some(k=>lower.includes(k)));return{applicant_label:lib.applicant_label,recipient_label:lib.recipient_label,applicant_obligations:p?.applicant||lib.defaults.applicant,recipient_obligations:p?.recipient||lib.defaults.recipient,obligation_rule_id:p?.id||null,affected_side:p?.affected_side||null,obligation_reference:p?.reference||null,obligation_source_url:p?.source_url||null,obligation_risk_level:p?.risk_level||null}}
