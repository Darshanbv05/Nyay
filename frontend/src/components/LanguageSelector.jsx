import { ui } from '../i18n/uiTranslations.js';
export default function LanguageSelector({languages,value,onChange,onAnalyze,onBack,documentName}){const selected=languages.find(item=>item.code===value),language=value||'en';return <section className="panel language-panel" aria-labelledby="language-title"><div className="step-label">{ui(language,'step2')}</div><button className="back" onClick={onBack}>← {ui(language,'changeDocument')}</button><h2 id="language-title">{ui(language,'chooseLanguage')}</h2><p className="muted">{selected?<>{ui(language,'ready')} <strong>{selected.label}</strong>. {ui(language,'languageNote')}</>:<><strong>{documentName}</strong>: {ui(language,'selectLanguage')}</>}</p><div className="language-grid">{languages.map(item=><button key={item.code} className={`language-option ${value===item.code?'selected':''}`} aria-pressed={value===item.code} onClick={()=>onChange(item.code)}><span>{item.label}</span><small>{item.code.toUpperCase()}</small></button>)}</div><div className="language-footer"><p>{selected?`${ui(language,'ready')} ${selected.label}.`:ui(language,'selectLanguage')}</p><button className="primary" disabled={!value} onClick={onAnalyze}>{ui(language,'analyze')} <span>→</span></button></div></section>}
