import{tr}from'../i18n/reportTranslations.js';export default function RiskDashboard({clauses,level,language='en'}){const risky=clauses.filter(c=>c.flagged).length,safe=clauses.length-risky;return <section className="risk-dashboard"><div><strong>{clauses.length}</strong><span>{tr(language,'total')}</span></div><div><strong>{risky}</strong><span>{tr(language,'risky')}</span></div><div><strong>{safe}</strong><span>{tr(language,'safeClauses')}</span></div><div><strong>{tr(language,level==='low'?'safe':level==='medium'?'negotiable':'unfair')}</strong><span>{tr(language,'overall')}</span></div><div className="heat-strip">{clauses.map(c=><i key={c.clause_id} className={c.flagged?(c.risk_level==='high'?'red':'yellow'):'green'}/>)}</div></section>}
