import{tr}from'../i18n/reportTranslations.js';export default function RiskBadge({level,large=false,language='en'}){const values={low:25,none:10,medium:60,high:90},key=level==='high'?'unfair':level==='medium'?'negotiable':'safe';return <div className={`severity ${large?'large':''}`}><div className="severity-head"><span>{tr(language,key)}</span><strong>{values[level]||0}%</strong></div><div className="severity-track"><i className={`risk-${level}`} style={{width:`${values[level]||0}%`}}/></div></div>}
