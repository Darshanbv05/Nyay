import{useState}from'react';
function Script({clause}){const[copied,setCopied]=useState(false);const copy=async()=>{await navigator.clipboard.writeText(clause.negotiation_script);setCopied(true);setTimeout(()=>setCopied(false),1500)};return <article><h3>{clause.source_label}</h3><p>💡 You can negotiate this clause. Suggested script available.</p><blockquote>{clause.negotiation_script}</blockquote><button className="secondary" onClick={copy}>{copied?'Copied':'Copy script'}</button></article>}
export default function NegotiationScriptsSection({clauses}){const items=clauses.filter(c=>c.flagged&&c.negotiation_script);return items.length?<div className="scripts-grid">{items.map(c=><Script key={c.clause_id} clause={c}/>)}</div>:<div className="dashboard-empty">No template negotiation script was triggered by the indexed patterns.</div>}
