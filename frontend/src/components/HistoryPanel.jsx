export default function HistoryPanel({ entries, onLoad, onClear }) { if (!entries.length) return null; return <aside className="history-panel"><div><span className="step-label">Demo-safe history</span><h2>Previous analyses</h2></div><div className="history-list">{entries.map(entry => <button key={entry.timestamp} onClick={() => onLoad(entry.report)}><strong>{entry.label}</strong><small>{new Date(entry.timestamp).toLocaleString()}</small></button>)}</div><button className="back" onClick={onClear}>Clear history</button></aside>; }
